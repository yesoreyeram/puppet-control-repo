<%- @storage_schemas.keys.sort.each do |key|
  if @storage_schemas[key].is_a?(Hash) -%>
[<%= key %>]<%- @storage_schemas[key].keys.sort.each do |valkey| -%>
<%= valkey %> = <%= @storage_schemas[key][valkey] %><%- end -%>
<%- end
end -%>
